# =============================================================================
# Semantic Search Frontend - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in the required values.
# =============================================================================

# -----------------------------------------------------------------------------
# API CONFIGURATION [REQUIRED]
# -----------------------------------------------------------------------------
# URL of the backend API server
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# -----------------------------------------------------------------------------
# DEBUG MODE
# -----------------------------------------------------------------------------
# Set to true to enable debug logging in browser console
# Set to false (or omit) for production
NEXT_PUBLIC_DEBUG=false

# Debug features when enabled:
# - API request/response logging
# - Component render logging
# - State change tracking
# - Performance metrics

# =============================================================================
# NOTES
# =============================================================================
#
# 1. All frontend environment variables must be prefixed with NEXT_PUBLIC_
#    to be accessible in the browser.
#
# 2. API keys should NEVER be stored in frontend environment variables.
#    All API keys are configured on the backend only.
#
# 3. The frontend communicates with the backend API, which handles:
#    - OpenAI API calls (embeddings, LLM)
#    - Cohere API calls (reranking)
#    - Jina API calls (embeddings)
#    - Database operations
#
# =============================================================================
